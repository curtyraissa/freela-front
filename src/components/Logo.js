import styled from "styled-components";

export default function Logo() {
  return <Text>Agência de Viagens ✈️ </Text>;
}

const Text = styled.h1`
  font-family: "Saira Stencil One", cursive;
  font-weight: 400;
  font-size: 30px;
  position: fixed;
  top: 125px;
`;
